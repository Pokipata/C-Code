#inclulde <stdio.h>

int Add(int a, int b)
{
    int c = a + b;
    return c;
}

int main ()
{
  int num1 = 0;
  int num2 = 0;
  int sum = 0;
  scanf("%d%d", &num1, &num2);
  sum = Add(num1, num2);
  printf("sum = %d\n", sum);
  return 0;
}
